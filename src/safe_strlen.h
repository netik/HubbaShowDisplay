// safe_strlen.h
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#ifndef SAFE_STRLEN_H
#define SAFE_STRLEN_H

// add prototype
size_t safe_strlen(const char *str, size_t max_len);

#endif
